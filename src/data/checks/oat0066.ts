import { Check } from "../../interfaces"

export const oat0066: Check = {
    check_ID: "oat0066",
    description: "Pages (including pages in iframes) must have non-empty title elements",
    checklist_IDs: [],
};
