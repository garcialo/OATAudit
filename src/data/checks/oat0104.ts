import { Check } from "../../interfaces";

export const oat0104: Check = {
	check_ID: "oat0104",
	description:
		'Names of elements and attributes must be free of errors (e.g. An "image" tag marked up as <imge> instead of <img> would be incorrect)',
	checklist_IDs: [],
};
