import { Check } from "../../interfaces"

export const oat0036: Check = {
    check_ID: "oat0036",
    description: "When auto-rotate setting is disabled (orientation is locked), content does not adjust to the device orientation unless it is considered essential",
    checklist_IDs: [],
};
