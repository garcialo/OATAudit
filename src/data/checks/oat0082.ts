import { Check } from "../../interfaces"

export const oat0082: Check = {
    check_ID: "oat0082",
    description: "Programmatic labels contain (or at least start with) the same text used in the visible label",
    checklist_IDs: [],
};
