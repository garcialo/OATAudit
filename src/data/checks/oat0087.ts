import { Check } from "../../interfaces"

export const oat0087: Check = {
    check_ID: "oat0087",
    description: "When an item receives keyboard focus, there must be no change of context (user agent, viewport, focus, changing content before the users current position on the page)",
    checklist_IDs: [],
};
