import { Check } from "../../interfaces"

export const oat0080: Check = {
    check_ID: "oat0080",
    description: "A simple single-pointer (e.g. finger) without a path (e.g. not dragging) gesture or button exists for each multiple-pointer, complex, or path-based gesture unless using a multiple-pointer, complex, or path-based gesture is essential",
    checklist_IDs: [],
};
