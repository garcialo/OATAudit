import { Check } from "../../interfaces"

export const oat0055: Check = {
    check_ID: "oat0055",
    description: "All interactive elements are reachable with keyboard (in the tab order, part of a widget, keyboard command, or have alternative method to do the same thing) and can be used with keyboard",
    checklist_IDs: [],
};
