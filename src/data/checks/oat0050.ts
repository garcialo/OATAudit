import { Check } from "../../interfaces"

export const oat0050: Check = {
    check_ID: "oat0050",
    description: "Visual information required to identify UI components (e.g. buttons, inputs) or understand graphical content (e.g. image icons) has a color contrast ratio at least 3:1 except when essential",
    checklist_IDs: [],
};
