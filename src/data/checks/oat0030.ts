import { Check } from "../../interfaces"

export const oat0030: Check = {
    check_ID: "oat0030",
    description: "Groups of related native inputs must have programmatic group labels (especially radio and checkbox groups)",
    checklist_IDs: [],
};
