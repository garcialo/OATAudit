import { Check } from "../../interfaces"

export const oat0071: Check = {
    check_ID: "oat0071",
    description: "Focus order must closely (but not necessarily exactly) follow the visual design of the page",
    checklist_IDs: [],
};
