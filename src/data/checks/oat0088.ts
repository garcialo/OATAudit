import { Check } from "../../interfaces"

export const oat0088: Check = {
    check_ID: "oat0088",
    description: "When a component receives input, there must be no change of context (user agent, viewport, focus, changing content before the users current position on the page)",
    checklist_IDs: [],
};
