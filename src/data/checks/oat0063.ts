import { Check } from "../../interfaces"

export const oat0063: Check = {
    check_ID: "oat0063",
    description: "Auto-updating content (like a slideshow banner) that starts automatically and is presented alongside other content must have a method to pause, stop, or hide the auto-updating content.",
    checklist_IDs: [],
};
