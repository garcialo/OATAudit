import { Check } from "../../interfaces"

export const oat0075: Check = {
    check_ID: "oat0075",
    description: "Link text makes sense either on its own or when combined with context from its sentence, paragraph, preceding heading, parent list item, or other content within its data cell when combined with its associated header cell.",
    checklist_IDs: [],
};
